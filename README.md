# Game Steps

To run follow next instructions in terminal:

```sh
cd game
python3 main.py
```

# App project

```sh
git clone
cd app
source env/bon/activate
pip install -r requirements.txt
python3 main.py
```
